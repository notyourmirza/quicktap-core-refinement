<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="QuickTap.App.Views.ProductsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:entities="using:QuickTap.Core.Entities">

    <Grid ColumnSpacing="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="360" />
        </Grid.ColumnDefinitions>

        <Grid RowSpacing="14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Products" Style="{StaticResource PageTitleStyle}" />
            <TextBox Grid.Row="1" PlaceholderText="Search products"
                     Text="{x:Bind ViewModel.Search, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

            <Border Grid.Row="2" Style="{StaticResource CardStyle}">
                <ListView ItemsSource="{x:Bind ViewModel.Items}"
                          SelectedItem="{x:Bind ViewModel.Selected, Mode=TwoWay}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="entities:Product">
                            <Grid Padding="0,8" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                                <TextBlock Grid.Column="1" Opacity="0.7" Text="{x:Bind Stock}" />
                                <TextBlock Grid.Column="2" FontWeight="SemiBold"
                                           Text="{x:Bind Price, Converter={StaticResource MoneyConverter}}" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Border>
        </Grid>

        <Border Grid.Column="1" Style="{StaticResource CardStyle}">
            <ScrollViewer>
                <StackPanel Spacing="10">
                    <TextBlock Text="Edit product" Style="{StaticResource SectionTitleStyle}" />
                    <TextBox Header="Name" Text="{x:Bind ViewModel.Selected.Name, Mode=TwoWay}" />
                    <NumberBox Header="Price" Value="{x:Bind ViewModel.Selected.Price, Mode=TwoWay}" />
                    <NumberBox Header="Cost price" Value="{x:Bind ViewModel.Selected.CostPrice, Mode=TwoWay}" />
                    <NumberBox Header="Tax %" Value="{x:Bind ViewModel.Selected.TaxPercent, Mode=TwoWay}" />
                    <NumberBox Header="Stock (-1 = untracked)" Value="{x:Bind ViewModel.Selected.Stock, Mode=TwoWay}" />
                    <TextBox Header="Barcode" Text="{x:Bind ViewModel.Selected.Barcode, Mode=TwoWay}" />
                    <TextBox Header="SKU" Text="{x:Bind ViewModel.Selected.Sku, Mode=TwoWay}" />
                    <ToggleSwitch Header="Available" IsOn="{x:Bind ViewModel.Selected.Available, Mode=TwoWay}" />

                    <Button Style="{StaticResource PrimaryActionStyle}" Content="Save"
                            Command="{x:Bind ViewModel.SaveCommand}" />
                    <Button Content="New product" HorizontalAlignment="Stretch" Command="{x:Bind ViewModel.NewCommand}" />
                    <Button Content="Delete" HorizontalAlignment="Stretch" Command="{x:Bind ViewModel.DeleteCommand}" />
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
